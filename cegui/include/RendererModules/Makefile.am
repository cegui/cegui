if BUILD_OPENGL_RENDERER
OPENGL=OpenGL
endif
if BUILD_OGRE_RENDERER
OGRE=Ogre
endif
if BUILD_IRRLICHT_RENDERER
IRRLICHT=Irrlicht
endif
if BUILD_DIRECTFB_RENDERER
DIRECTFB=DirectFB
endif
if BUILD_NULL_RENDERER
NULLREND=Null
endif
SUBDIRS = . $(OPENGL) $(OGRE) $(IRRLICHT) $(DIRECTFB) \
          $(NULLREND) Direct3D9 Direct3D10 Direct3D11
