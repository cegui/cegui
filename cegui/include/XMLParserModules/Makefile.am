if BUILD_XERCES_PARSER
XERCESDIR=XercesParser
endif
if BUILD_EXPAT_PARSER
EXPATDIR=ExpatParser
endif
if BUILD_LIBXML_PARSER
LIBXMLDIR=LibxmlParser
endif
if BUILD_RAPIDXML_PARSER
RAPIDXMLDIR=RapidXMLParser
endif
if BUILD_TINYXML_PARSER
TINYXMLDIR=TinyXMLParser
endif
SUBDIRS = . $(XERCESDIR) $(EXPATDIR) $(LIBXMLDIR) $(RAPIDXMLDIR) $(TINYXMLDIR)
